apiVersion: v1
kind: Secret
metadata:
  name: {{ include "multica-runtime-controller.fullname" . }}-worker-config
  labels:
    {{- include "multica-runtime-controller.labels" . | nindent 4 }}
type: Opaque
stringData:
  worker.json: {{ include "multica-runtime-controller.workerConfig" . | quote }}
